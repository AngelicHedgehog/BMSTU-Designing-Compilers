*$FROM LibraryEx
*$FROM Lexer
*$FROM Parser
$EXTERN ArgList, LoadFile, Lexer, Parse;

$ENTRY Go {
    /* пусто */ = <Main <ArgList>>
}

Main {
    (e.ProgName) (e.InputFile)
        , <Prout <Parse <Lexer <LoadFile e.InputFile>>>> :
        = /* пусто */;
}
